--- 
spring: 
  application: 
    name: company
  cloud: 
    config: 
      uri: ${vcap.services.configServer.credentials.uri}

management:
  security:
    enabled: false

security:
  basic:
    enabled: false
